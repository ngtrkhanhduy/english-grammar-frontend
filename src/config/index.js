import routes from './routes';
import { MenuItem_Learning, MenuItem_Document } from './sidebar';

const config = {
    routes,
    MenuItem_Learning,
    MenuItem_Document,
};

export default config;
